# Copilot Instructions

# Nuxt UI
- Use Nuxt UI for all components; only after checking the docs for components that are not available in Nuxt UI, build your own component
- Use Tailwind CSS for styling
- Use Pinia for state management
- Use Supabase for authentication and database